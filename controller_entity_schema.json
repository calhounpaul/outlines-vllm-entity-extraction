{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "C# Controller File Entity Extraction Schema",
    "type": "object",
    "definitions": {
      "entity": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the entity."},
          "type": {"type": "string", "description": "The type of the entity."},
          "description": {"type": "string", "description": "A brief description of the entity."}
        },
        "required": ["name", "type"]
      },
      "method": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the method."},
          "type": {"type": "string", "description": "The type of the method."},
          "description": {"type": "string", "description": "A brief description of the method."},
          "httpMethod": {
            "type": "string",
            "enum": ["GET", "POST", "PUT", "DELETE"],
            "description": "The HTTP method used by the action."
          },
          "route": {"type": "string", "description": "The route associated with the method."}
        },
        "required": ["name", "type", "httpMethod", "route"]
      },
      "propertyDeclaration": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the property."},
          "type": {"type": "string", "description": "The type of the property."},
          "description": {"type": "string", "description": "A brief description of the property."},
          "accessibility": {
            "type": "string",
            "enum": ["Public", "Private", "Read-Only"],
            "description": "The accessibility of the property."
          }
        },
        "required": ["name", "type", "accessibility"]
      },
      "dataOperation": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the data operation."},
          "type": {"type": "string", "description": "The type of the data operation."},
          "description": {"type": "string", "description": "A brief description of the data operation."},
          "operationType": {
            "type": "string",
            "enum": ["Retrieve", "Insert", "Update", "Delete"],
            "description": "The type of data operation."
          },
          "target": {"type": "string", "description": "The target entity of the data operation."}
        },
        "required": ["name", "type", "operationType", "target"]
      },
      "errorHandling": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the error handling mechanism."},
          "type": {"type": "string", "description": "The type of the error handling mechanism."},
          "description": {"type": "string", "description": "A brief description of the error handling mechanism."},
          "mechanism": {
            "type": "string",
            "enum": ["Try-Catch", "CustomException", "Logging"],
            "description": "The type of error handling mechanism."
          }
        },
        "required": ["name", "type", "mechanism"]
      },
      "validation": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the validation procedure."},
          "type": {"type": "string", "description": "The type of the validation procedure."},
          "description": {"type": "string", "description": "A brief description of the validation procedure."},
          "validationType": {
            "type": "string",
            "enum": ["Input", "Response"],
            "description": "The type of validation being performed."
          }
        },
        "required": ["name", "type", "validationType"]
      },
      "configuration": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the configuration setting."},
          "type": {"type": "string", "description": "The type of the configuration setting."},
          "description": {"type": "string", "description": "A brief description of the configuration setting."},
          "setting": {
            "type": "string",
            "enum": ["ServiceInjection", "AppSettings", "EnvironmentVariables"],
            "description": "The type of configuration setting."
          }
        },
        "required": ["name", "type", "setting"]
      },
      "miscellaneous": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the miscellaneous item."},
          "type": {"type": "string", "description": "The type of the miscellaneous item."},
          "description": {"type": "string", "description": "A brief description of the miscellaneous item."},
          "itemType": {
            "type": "string",
            "enum": ["UtilityMethod", "HelperFunction", "Documentation", "Logging", "EventHandling"],
            "description": "The type of miscellaneous item."
          }
        },
        "required": ["name", "type", "itemType"]
      },
      "classDeclaration": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the class."},
          "type": {"type": "string", "description": "The type of the class."},
          "description": {"type": "string", "description": "A brief description of the class."}
        },
        "required": ["name", "type"]
      },
      "constantDeclaration": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the constant."},
          "type": {"type": "string", "description": "The type of the constant."},
          "description": {"type": "string", "description": "A brief description of the constant."}
        },
        "required": ["name", "type"]
      },
      "enumDeclaration": {
        "type": "object",
        "properties": {
          "name": {"type": "string", "description": "The name of the enum."},
          "type": {"type": "string", "description": "The type of the enum."},
          "description": {"type": "string", "description": "A brief description of the enum."}
        },
        "required": ["name", "type"]
      }
    },
    "properties": {
      "declarationsAndDefinitions": {
        "type": "object",
        "properties": {
          "classDeclarations": {
            "type": "array",
            "items": {"$ref": "#/definitions/classDeclaration"},
            "description": "List of class declarations with their types."
          },
          "methodDeclarations": {
            "type": "array",
            "items": {"$ref": "#/definitions/method"},
            "description": "List of method declarations with their types and routes."
          },
          "propertyDeclarations": {
            "type": "array",
            "items": {"$ref": "#/definitions/propertyDeclaration"},
            "description": "List of property declarations with their accessibility."
          },
          "constantDeclarations": {
            "type": "array",
            "items": {"$ref": "#/definitions/constantDeclaration"},
            "description": "List of constant declarations with their types."
          },
          "enumDeclarations": {
            "type": "array",
            "items": {"$ref": "#/definitions/enumDeclaration"},
            "description": "List of enum declarations with their types."
          }
        },
        "required": [
          "classDeclarations",
          "methodDeclarations",
          "propertyDeclarations",
          "constantDeclarations",
          "enumDeclarations"
        ],
        "description": "Declarations and definitions of classes, methods, properties, constants, and enums."
      },
      "httpMethodsAndRoutes": {
        "type": "object",
        "properties": {
          "methods": {
            "type": "array",
            "items": {"$ref": "#/definitions/method"},
            "description": "List of HTTP methods and their routes."
          },
          "routes": {
            "type": "array",
            "items": {"type": "string"},
            "description": "List of routes defined in the controller."
          }
        },
        "required": ["methods", "routes"],
        "description": "HTTP methods and routes used in the controller."
      },
      "dataAccessAndManipulation": {
        "type": "object",
        "properties": {
          "dataOperations": {
            "type": "array",
            "items": {"$ref": "#/definitions/dataOperation"},
            "description": "List of data operations including retrieval, insertion, update, and deletion."
          }
        },
        "required": ["dataOperations"],
        "description": "Data access and manipulation operations performed by the controller."
      },
      "errorHandlingAndValidation": {
        "type": "object",
        "properties": {
          "errorHandling": {
            "type": "array",
            "items": {"$ref": "#/definitions/errorHandling"},
            "description": "List of error handling mechanisms used in the controller."
          },
          "validation": {
            "type": "array",
            "items": {"$ref": "#/definitions/validation"},
            "description": "List of validation procedures including input and response validation."
          }
        },
        "required": ["errorHandling", "validation"],
        "description": "Error handling and validation procedures used in the controller."
      },
      "configurationAndServices": {
        "type": "object",
        "properties": {
          "configurations": {
            "type": "array",
            "items": {"$ref": "#/definitions/configuration"},
            "description": "List of configuration settings and service injections."
          }
        },
        "required": ["configurations"],
        "description": "Configuration settings and services used in the controller."
      },
      "miscellaneous": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {"$ref": "#/definitions/miscellaneous"},
            "description": "List of miscellaneous items including utility methods, documentation, and event handling."
          }
        },
        "required": ["items"],
        "description": "Miscellaneous items related to the controller."
      }
    },
    "required": [
      "declarationsAndDefinitions",
      "httpMethodsAndRoutes",
      "dataAccessAndManipulation",
      "errorHandlingAndValidation",
      "configurationAndServices",
      "miscellaneous"
    ],
    "description": "Schema for extracting and organizing C# controller file entities."
}